{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://emberveil.rsg/schemas/v1/classes.json",
  "title": "Emberveil Classes Document (v1)",
  "description": "Canonical class data document. Source-of-truth for src/game/classes.js (CLASSES base literal + CLASS_TAGS). Logic (unlockRequirement/tags attachment, classHasTag, lookups) stays in classes.js.",
  "type": "object",
  "required": ["version", "source", "classes", "classTags"],
  "additionalProperties": false,
  "properties": {
    "version": { "const": 1 },
    "source":  { "type": "string" },
    "classes": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "https://emberveil.rsg/schemas/v1/class.json" }
    },
    "classTags": {
      "type": "object",
      "description": "Map of class id → flat tag string array. Drives encounter/quest/event gating.",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  }
}
